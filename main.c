#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int antGirl = 5;
	int boyGuys = 6;
	int s;
	s = antGirl + boyGuys;
	printf("%d + %d = %d",antGirl,boyGuys,s);
	return 0;
}
